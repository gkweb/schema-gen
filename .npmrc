# Use exact versions for reproducibility
save-exact=true

# Hoist all dependencies for better compatibility
shamefully-hoist=true

# Required for native modules
node-linker=hoisted

# Publishing configuration for scoped packages
@schema-gen:registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
